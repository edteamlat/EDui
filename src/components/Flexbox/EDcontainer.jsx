import styled from 'styled-components';

export const EDcontainer = styled.div`
	display      : flex;
  flex-wrap    : wrap;
  max-width    : var(--max-width);
  width        : 100%;
  margin-left  : auto;
  margin-right : auto;
`;
